console.log("hao");
/* var mapa = new L.map('mapa_id').setView([46.05004,14.46932], 9);

    var layer = new L.TileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png');

    mapa.addLayer(layer);
    var popup = L.popup();
    
    function klik(e) {
      var latlng = e.latlng;
      popup
        .setLatLng(latlng)
        .setContent("lokacija: " + latlng.toString()))
        .openOn(mapa);
        prikazPoti(latlng);
    }
    mapa.on('click', klik);*/